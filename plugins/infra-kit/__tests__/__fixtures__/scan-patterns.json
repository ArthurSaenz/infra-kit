{
  "_why": "U5 and T5 scan every file under plugins/, the scanner included. Plan U5 says __tests__ is never exempt, so the needles cannot sit inline in manifest.test.mjs. They live here, in the one directory the scan exempts, split into fragments the test joins. Splitting them keeps an external grep over plugins/ clean too: neither a whole denied name nor a whole project-relative prefix appears as a literal in any file the plugin ships, so a grep-based check sees no self-hit either. Do not spell any needle out in this file.",
  "projectRelative": [
    [".claude/", "skills/"],
    [".claude/", "hooks/"]
  ],
  "denylist": [
    ["hul", "yo"],
    ["trave", "list"],
    ["gulli", "ver"]
  ],
  "legacyToolPrefix": [["mcp__", "infra-kit__"]],
  "_pluginToolNameWhy": "The ONE definition of 'names a plugin-served tool': a regex source whose first group is the bare tool name. Read by manifest.test.mjs (U14', U18), by scripts/report-published-cli-skew.mjs (collectSkew) and by the CLI's command-catalog.test.ts cross-check, so the three cannot drift on what counts as naming a tool. Not fragmented: the plugin prefix is the LIVE spelling every skill may name, not a needle any scan denies.",
  "pluginToolName": "mcp__plugin_infra-kit_infra-kit__([a-z][a-z0-9-]*)"
}
