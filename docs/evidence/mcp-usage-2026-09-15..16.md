# Evidence — the retired MCP server was still serving tool calls after the skills shipped

Driver D1 of `.omc/plans/mcp-phase3-deletion-decision.md`. Measured 2026-09-17 over the Claude Code
transcripts of `~/projects/hulyo-monorepo` (`~/.claude/projects/-Users-arthur-projects-hulyo-monorepo/*.jsonl`).
Counted: `tool_use` blocks only (no `tool_result`), grouped by the message `timestamp` date (UTC).

Publish times (`pnpm view infra-kit time`): 0.8.0 (plugin skills, `.mcp.json` dropped from the plugin)
2026-09-16T07:59Z; 0.9.0 2026-09-16T20:46Z.

| Date (UTC) | Family                                      | Tool                                                               | Calls |
| ---------- | ------------------------------------------- | ------------------------------------------------------------------ | ----- |
| 2026-09-15 | plugin `mcp__plugin_infra-kit_infra-kit__*` | release-remove 10 · gh-merge-dev 3 · env-load 2 · release-create 2 | 17    |
| 2026-09-15 | legacy `mcp__infra-kit__*`                  | —                                                                  | 0     |
| 2026-09-16 | legacy `mcp__infra-kit__*`                  | env-load 15 · release-create 3 · worktrees-add 1 · env-list 1      | 20    |
| 2026-09-16 | plugin                                      | —                                                                  | 0     |

Last legacy call: 2026-09-16T21:54Z — after both the 0.8.0 and the 0.9.0 publish. `release-create` ×3 and
`worktrees-add` ×1 went through the server's confirm gate (memory: the gate runs before the handler and is
blind to arguments), not through the host's permission prompt on the argv.

The plan's §0 quotes 15 legacy calls on 09-16 and 43 on 09-15 from the Critic's pass; that pass counted
differently (it is not reproducible from this script). The numbers above are the ones to re-run.

## PM4 re-run (+7 days after the 0.10.0 publish)

Expected: legacy = 0 on every post-publish day; plugin/skill invocations > 0. Append the table below.

```sh
cd ~/.claude/projects/-Users-arthur-projects-hulyo-monorepo && node -e '
const fs=require("fs");const out={};
for(const f of fs.readdirSync(".").filter(f=>f.endsWith(".jsonl"))){
  for(const l of fs.readFileSync(f,"utf8").split("\n")){
    if(!l.includes("mcp__infra-kit__")&&!l.includes("mcp__plugin_infra-kit")) continue;
    let j; try{j=JSON.parse(l)}catch{continue}
    const ts=(j.timestamp||"").slice(0,10); const c=j.message?.content; if(!Array.isArray(c)) continue;
    for(const b of c){ if(b.type!=="tool_use") continue; const n=b.name||"";
      const fam=n.startsWith("mcp__infra-kit__")?"legacy":n.startsWith("mcp__plugin_infra-kit")?"plugin":null; if(!fam) continue;
      const k=`${ts} ${fam} ${n}`; out[k]=(out[k]||0)+1; } } }
for(const [k,v] of Object.entries(out).sort()) console.log(v.toString().padStart(3), k);'
```
