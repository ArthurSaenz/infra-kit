# Keeps the SHA-pinned actions current. A compromised action release is usually pulled within
# days; 7 is the floor zizmor's dependabot-cooldown audit accepts.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    cooldown:
      default-days: 7
