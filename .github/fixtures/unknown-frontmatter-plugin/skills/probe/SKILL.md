---
name: probe
description: U13 probe fixture — carries OMC-runtime frontmatter fields that Claude Code does not define, so CI can report whether `claude plugin validate --strict` objects to them. Never installed.
aliases: [probe-alias]
pipeline: ralplan
next-skill: ralph
---

This skill is a validation probe. It does nothing.
